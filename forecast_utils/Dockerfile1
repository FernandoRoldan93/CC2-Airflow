FROM python:3.6.10-slim-stretch

ENV PORT 8081

COPY . ./

RUN apt-get update && pip install --upgrade pip && pip install --requirement requirements.txt && ping 172.17.0.2

EXPOSE 8081
EXPOSE 22

CMD gunicorn --workers=4 -b 0.0.0.0:8081 api_arima:app
